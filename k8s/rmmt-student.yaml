apiVersion: apps/v1
kind: Deployment
metadata:
  name: rmmt-student
  namespace: rmmt
  labels:
    app: rmmt-student
spec:
  replicas: 2
  selector:
    matchLabels:
      app: rmmt-student
  template:
    metadata:
      labels:
        app: rmmt-student
    spec:
      containers:
      - name: rmmt-student
        image: rmmt-student:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 80
          name: http
        env:
        - name: NUXT_API_URL
          value: "https://rmmt-api.rmmt.svc.cluster.local"
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      restartPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: rmmt-student
  namespace: rmmt
  labels:
    app: rmmt-student
spec:
  selector:
    app: rmmt-student
  ports:
  - name: http
    port: 80
    targetPort: 80
    protocol: TCP
  - name: https
    port: 443
    targetPort: 80
    protocol: TCP
  type: ClusterIP