apiVersion: v1
kind: ConfigMap
metadata:
  name: rmmt-config
  namespace: rmmt
data:
  # 数据库配置
  DB_HOST: "rmmt-db.rmmt.svc.cluster.local"
  DB_PORT: "3306"
  DB_NAME: "roommate"
  DB_USER: "rmmt"
  DB_PASSWORD: "password123"
  # 应用配置
  NODE_ENV: "production"
  LOG_LEVEL: "info"